# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Personal/sensitive data — never commit these
profile.json
testoutput/
*.xlsx
# ...except the blank Excel template itself — same category as
# OF288-Fillable.pdf, a safe non-personal form template, not payroll data.
!ref/OF288 Excel Blank Template.xlsx
USDA FS Paycheck8*.pdf
exsamples/
paystubs !notpublic!/

# Editor/office lock files
.~lock.*#
